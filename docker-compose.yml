version: '3.8'

services:
  backend:
    image: webcode-backend
    expose:
      - "8000"
    restart: unless-stopped

  frontend:
    image: webcode-frontend
    ports:
      - "3000:80"
    depends_on:
      - backend
    restart: unless-stopped
    labels:
      - "sandbox.expose: true"